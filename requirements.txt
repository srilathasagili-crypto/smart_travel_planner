streamlit==1.38.0
pydeck==0.9.1
pandas==2.2.2
googlemaps==4.10.0
python-dotenv==1.0.1
spacy==3.7.5
anthropic==0.34.2

# Streamlit Cloud only runs `pip install -r requirements.txt` - there's no separate
# build-command step like on Render/Railway, so the spaCy English model has to be
# installed as a regular pip package (a direct wheel URL) rather than via
# `python -m spacy download en_core_web_sm`.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
